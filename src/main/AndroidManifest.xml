<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.assignmentfinal">  <!-- FIX #1: Added the package name -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AssignmentFinal"
        tools:targetApi="34"> <!-- FIX #2: Updated to a modern SDK version -->

        <!-- This is your main launcher activity -->
        <activity
            android:name=".activity_main"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declaration for QuestionActivity -->
        <activity
            android:name=".QuestionActivity"
            android:exported="false" />

        <!-- Declaration for ScoreActivity -->
        <activity
            android:name=".ScoreActivity"
            android:exported="false" />

    </application>

</manifest>
